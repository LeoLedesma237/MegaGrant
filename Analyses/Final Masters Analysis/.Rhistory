)
alpha = 0.05
data %>%
mutate(term = fct_inorder(term)) %>%
group_by(term) %>%
summarise(
effect = first(effect),
sim_power = p.value < alpha,
sim_n = n(),
.groups = "drop"
)
data %>%
mutate(term = fct_inorder(term)) %>%
group_by(term)
data %>%
mutate(term = fct_inorder(term)) %>%
group_by(term) %>%
summarise(
effect = first(effect),
sim_power = mean(p.value < alpha),
sim_n = n(),
.groups = "drop"
)
data %>%
mutate(term = fct_inorder(term)) %>%
group_by(term, dup_n) %>%
summarise(
effect = first(effect),
sim_power = mean(p.value < alpha),
sim_n = n(),
.groups = "drop"
)
data %>%
mutate(term = fct_inorder(term)) %>%
group_by(term, dup_n) %>%
summarise(
effect = first(effect),
sim_power = mean(p.value < alpha),
sim_n = n(),
.groups = "drop"
) %>%
ggplot(aes(x = dup_n, y = sim_power)) +
geom_point() +
facet_wrap(~term) +
theme_minimal()
data
data %>%
filter(effect == "fixed") %>%
mutate(term = fct_inorder(term)) %>%
group_by(term, dup_n) %>%
summarise(
effect = first(effect),
sim_power = mean(p.value < alpha),
sim_n = n(),
.groups = "drop"
) %>%
ggplot(aes(x = dup_n, y = sim_power)) +
geom_point() +
facet_wrap(~term) +
theme_minimal()
data %>%
filter(effect == "fixed") %>%
mutate(term = fct_inorder(term)) %>%
group_by(term, dup_n) %>%
summarise(
effect = first(effect),
sim_power = mean(p.value < alpha),
sim_n = n(),
.groups = "drop"
) %>%
ggplot(aes(x = dup_n, y = sim_power, group = term)) +
geom_point() +
geom_line() +
facet_wrap(~term) +
theme_minimal()
data %>%
filter(effect == "fixed") %>%
mutate(term = fct_inorder(term),
dup_n = factor(dup_n)) %>%
group_by(term, dup_n) %>%
summarise(
effect = first(effect),
sim_power = mean(p.value < alpha),
sim_n = n(),
.groups = "drop"
) %>%
ggplot(aes(x = dup_n, y = sim_power, group = term)) +
geom_point() +
geom_line() +
facet_wrap(~term) +
theme_minimal()
# data summary
data_sum <- data %>%
filter(effect == "fixed") %>%
mutate(term = fct_inorder(term),
dup_n = factor(dup_n)) %>%
group_by(term, dup_n) %>%
summarise(
effect = first(effect),
sim_power = mean(p.value < alpha),
sim_n = n(),
.groups = "drop"
)
data_sum$sim_n
data_sum
# Create some plots
plot_power <- function(data, group, alpha = 0.05) {
# data summary
data_sum <- data %>%
filter(effect == "fixed") %>%
mutate(term = fct_inorder(term),
dup_n = factor(dup_n)) %>%
group_by(term, dup_n) %>%
summarise(
effect = first(effect),
sim_power = mean(p.value < alpha),
sim_n = n(),
.groups = "drop"
)
# Generate a plot
p1 <- data_sum %>%
ggplot(aes(x = dup_n, y = sim_power, group = term)) +
geom_point() +
geom_line() +
facet_wrap(~term) +
theme_minimal()
# Return the plot and the sample size per group
list(p1, unique(select(data_sum, dup_n, sim_n)))
}
# Create some plots
plot_power <- function(data, group, alpha = 0.05) {
# data summary
data_sum <- data %>%
filter(effect == "fixed") %>%
mutate(term = fct_inorder(term),
group = factor(group)) %>%
group_by(term, group) %>%
summarise(
effect = first(effect),
sim_power = mean(p.value < alpha),
sim_n = n(),
.groups = "drop"
)
# Generate a plot
p1 <- data_sum %>%
ggplot(aes(x = group, y = sim_power, group = term)) +
geom_point() +
geom_line() +
facet_wrap(~term) +
theme_minimal()
# Return the plot and the sample size per group
list(p1, unique(select(data_sum, group, sim_n)))
}
plot_power(data, dup_n, 0.05)
# Read in the data
data <- read_csv("sims_N2.csv")
str(data)
# Create some plots
plot_power <- function(data, group, alpha = 0.05) {
# data summary
data_sum <- data %>%
filter(effect == "fixed") %>%
mutate(term = fct_inorder(term),
group = factor(group)) %>%
group_by(term, group) %>%
summarise(
effect = first(effect),
sim_power = mean(p.value < alpha),
sim_n = n(),
.groups = "drop"
)
# Generate a plot
p1 <- data_sum %>%
ggplot(aes(x = group, y = sim_power, group = term)) +
geom_point() +
geom_line() +
facet_wrap(~term) +
theme_minimal()
# Return the plot and the sample size per group
list(p1, unique(select(data_sum, group, sim_n)))
}
plot_power(data, dup_n, 0.05)
# Create a custom function to produce power as a function of sample size plots
plot_power <- function(data, group, alpha = 0.05) {
# data summary
data_sum <- data %>%
filter(effect == "fixed") %>%
mutate(
term = fct_inorder(term),
group = factor({{ group }}) # <-- Added {{ }} here!
) %>%
group_by(term, group) %>%
summarise(
effect = first(effect),
sim_power = mean(p.value < alpha),
sim_n = n(),
.groups = "drop"
)
# Generate a plot
p1 <- data_sum %>%
ggplot(aes(x = group, y = sim_power, group = term)) +
geom_point() +
geom_line() +
facet_wrap(~term) +
theme_minimal()
# Return the plot and the sample size per group
list(p1, unique(select(data_sum, group, sim_n)))
}
# Create the power plot
plot_power(data, dup_n, 0.05)
# Create the power plot
plot_power(data, "dup_n", 0.05)
# Create the power plot
plot_power(data, dup_n, 0.05)
library(readr)
library(tidyverse)
# set working directory
setwd("~/GitHub/MegaGrant/Analyses/Final Masters Analysis/sim")
# Read in the data
data <- read_csv("sims_N2.csv")
# Create the power plot
plot_power(data, dup_n, 0.05)
50000/2000
seq(1000, 50000, by = 2500)
single_run
library(tidyverse)
library(ggplot2)
library(nlme)
library(here)
library(forcats) # fct_inorder()
library(nlraa) # SSquadp3xs()
set.seed(123)
source(here("functions.R"))
# Read in the best fitting delta model
delta_mod <- readRDS(here("mods", "delta_mod.rds"))$mod2
# Extract the fixed effects from it
fixe_effs <- fixef(delta_mod)
# Extract the random effects from it
rand_effs <- as.numeric(VarCorr(delta_mod)[,"StdDev"])
tau_0 <- rand_effs[1]
sigma <- rand_effs[2]
# Save the original dataset
delta_dat <- delta_mod$data
delta_dat$FB <- as.character(delta_dat$FB )
# Get the subject level variables
delta_dat_agg <- delta_dat %>%
select(ID, FB, Spell_group, Sex, Age_min_c, Age, Age_group) %>%
mutate(Age_group_c = var_group(Age_min_c, 5)) %>%
unique()
# Remind the factor levels
lapply(delta_dat_agg, levels)
# Some data descriptives
get_descriptives(delta_dat_agg)
# Investigate the contrasts per factor for raw data
contrasts(delta_dat$Spell_group)
contrasts(delta_dat$Sex)
contrasts(delta_dat$region)
contrasts(delta_dat$hemisphere)
# Obtain a dataframe of between subject factors
subjects <- between_subjects()
# Add age information from the sample
subjects <- add_age(subjects, "empirical", delta_dat_agg)
# Add subject level effects
subjects$a_ran <- rnorm(nrow(subjects), 0, tau_0)
# Get descriptives of the information
get_descriptives(subjects)
# Produce a dataframe of the crossed within subject factors
crossed_top <- expand.grid(region = unique(delta_dat$region),
hemisphere = unique(delta_dat$hemisphere))
# Print to show the crossed data frame
crossed_top
# Introduce the within subject factors to the subject data
subjects_crossed <- crossing(subjects, crossed_top)
# View information about the crossed data frame
xtabs(~hemisphere + region , subjects_crossed)
# Run the function
subjects_crossed_design <- create_design_variables(subjects_crossed)
# Keep the design matrix for plotting purposes
design_matrix <- subjects_crossed_design %>%
arrange(Spell_group, Sex, ID, hemisphere, region) %>%
mutate(intercept = 1) %>%
select(intercept, SexM:hem_parietal)
# View the first few rows
head(design_matrix)
# Plot the design matrix
plot_design(design_matrix)
# Set the correct contrasts to the simulated data
subjects_crossed_design_contrasts <- set_model_contrasts(subjects_crossed_design)
# Investigate the contrasts per factor for the dataframe that will be used to simulate data
contrasts(subjects_crossed_design_contrasts$Spell_group)
contrasts(subjects_crossed_design_contrasts$Sex)
contrasts(subjects_crossed_design_contrasts$region)
contrasts(subjects_crossed_design_contrasts$hemisphere)
# Extract the parameter fixed effects
beta_a  = fixe_effs[1:12]
beta_b  = fixe_effs[13:14]
beta_xs = fixe_effs[15:16]
# Get the simulated outcome
sim_data <- add_simulated_outcome(
df      = subjects_crossed_design_contrasts,
beta_a  = beta_a,
beta_b  = beta_b,
beta_xs = beta_xs,
sigma   = sigma
)
# Viewing design matrix
#sim_data %>%
#  mutate(Intercept = 1) %>%
#  select(ID, Intercept,  SexM:log_absolute_power) %>%
#  mutate(across(where(is.numeric), ~ round(., 3))) %>%
#  head(30) %>%
#  view()
# Run the data simulation function
my_sim_data()
# Set seed to a seed that works
set.seed(420)
# Simulate data
dat_sim <- my_sim_data()
# Fit a QTPME
fit <- fit_sim_model(dat_sim)$model
# Extract model estimates
sim_results <- extract_sim_results(fit)
single_run
# Create the file name for the sensitivity analysis
filename_recovery <- here("sim", "sims_recovery.csv")
# Set the number of simulations per change in join point value
nreps <- 1400
# Create the parameter dataframe
params <- crossing(
iteration = 1:nreps
) %>%
mutate(seed = iteration)
# Simulate the data changing the sample size by 100 every 800 repetition
if (!file.exists(filename_recovery)) {
# Run the single_run() function using the params df as inputs
sims_sensitivity <- purrr::pmap_df(
params,
single_run,
filename = filename_recovery
)
}
# Read in the .csv file
sims_recovery <- read_csv(filename_recovery, show_col_types  = FALSE)
# Create a vector for the original estimates
original_estimates <- c(fixe_effs, rand_effs)
# Create a summary table of QTPME parameter estimated from the simulated data
summarize_simulation(sims_recovery, alpha = 0.05, original_estimates)
# Create a summary table of the variability in the estimates from the simulated data
summarize_sim_dist(sims_recovery, original_estimates)
single_run
library(furrr)
filename_N <- here("sim", "sims_N.csv")
nreps <- 1200
params <- crossing(
iteration = 1:nreps,
n = seq(1000, 50000, by = 2500)
) %>%
mutate(seed = iteration)
# Control mode: TRUE for parallel (fast), FALSE for sequential (appends per run)
use_parallel <- TRUE
if (!file.exists(filename_N)) {
if (use_parallel) {
# 1. PARALLEL MODE:
# Pass filename = NULL to bypass write_csv inside single_run and prevent file locking
plan(multisession, workers = availableCores() - 1)
sims_sensitivity <- future_pmap_dfr(
params,
single_run,
filename = NULL,
.options = furrr_options(seed = TRUE)
)
plan(sequential)
# Save the combined dataset once all workers complete
write_csv(sims_sensitivity, filename_N)
} else {
# 2. SEQUENTIAL MODE:
# Pass filename = filename_N so single_run appends row-by-row as before
sims_sensitivity <- purrr::pmap_df(
params,
single_run,
filename = filename_N
)
}
}
# Read in final output
sims_N <- read_csv(filename_N, show_col_types = FALSE)
library(furrr)
filename_N <- here("sim", "sims_N_parallel.csv")
nreps <- 1200
params <- crossing(
iteration = 1:nreps,
n = seq(1000, 50000, by = 2500)
) %>%
mutate(seed = iteration)
# Control mode: TRUE for parallel (fast), FALSE for sequential (appends per run)
use_parallel <- TRUE
if (!file.exists(filename_N)) {
if (use_parallel) {
# 1. PARALLEL MODE:
# Pass filename = NULL to bypass write_csv inside single_run and prevent file locking
plan(multisession, workers = availableCores() - 1)
sims_sensitivity <- future_pmap_dfr(
params,
single_run,
filename = NULL,
.options = furrr_options(seed = TRUE)
)
plan(sequential)
# Save the combined dataset once all workers complete
write_csv(sims_sensitivity, filename_N)
} else {
# 2. SEQUENTIAL MODE:
# Pass filename = filename_N so single_run appends row-by-row as before
sims_sensitivity <- purrr::pmap_df(
params,
single_run,
filename = filename_N
)
}
}
library(furrr)
filename_N <- here("sim", "sims_N_parallel.csv")
nreps <- 1200
params <- crossing(
iteration = 1:nreps,
n = seq(1000, 50000, by = 2500)
) %>%
mutate(seed = iteration)
nreps <- 50
params <- crossing(
iteration = 1:nreps,
n = seq(100, 300, by = 100)
) %>%
mutate(seed = iteration)
# Control mode: TRUE for parallel (fast), FALSE for sequential (appends per run)
use_parallel <- TRUE
if (!file.exists(filename_N)) {
if (use_parallel) {
# 1. PARALLEL MODE:
# Pass filename = NULL to bypass write_csv inside single_run and prevent file locking
plan(multisession, workers = availableCores() - 1)
sims_sensitivity <- future_pmap_dfr(
params,
single_run,
filename = NULL,
.options = furrr_options(seed = TRUE)
)
plan(sequential)
# Save the combined dataset once all workers complete
write_csv(sims_sensitivity, filename_N)
} else {
# 2. SEQUENTIAL MODE:
# Pass filename = filename_N so single_run appends row-by-row as before
sims_sensitivity <- purrr::pmap_df(
params,
single_run,
filename = filename_N
)
}
}
# Read in final output
sims_N <- read_csv(filename_N, show_col_types = FALSE)
# Specify the name of the file and number of iterations per crossing
filename_N <- here("sim", "sims_N_parallel.csv")
nreps <- 1200
# Specify the parameters
params <- crossing(
iteration = 1:nreps,
n = seq(1000, 50000, by = 2500)
) %>%
mutate(seed = iteration)
# Control mode: TRUE for parallel (fast), FALSE for sequential (appends per run)
use_parallel <- TRUE
if (!file.exists(filename_N)) {
if (use_parallel) {
# 1. PARALLEL MODE:
# Pass filename = NULL to bypass write_csv inside single_run and prevent file locking
plan(multisession, workers = availableCores() - 1)
sims_sensitivity <- future_pmap_dfr(
params,
single_run,
filename = NULL,
.options = furrr_options(seed = TRUE)
)
plan(sequential)
# Save the combined dataset once all workers complete
write_csv(sims_sensitivity, filename_N)
} else {
# 2. SEQUENTIAL MODE:
# Pass filename = filename_N so single_run appends row-by-row as before
sims_sensitivity <- purrr::pmap_df(
params,
single_run,
filename = filename_N
)
}
}
# Read in final output
sims_N <- read_csv(filename_N, show_col_types = FALSE)
problems(dat)
problems(sims_sensitivity)
sims_N
# Plot the power change as a function of increasing sample size
plot_power(sims_N, n, 0.05)
plot_power(sims_N, n, 0.05)
sims_N
sims_N %>%
sims_N$term %>% unique()
sims_N %>%
filter(term == "xs.Spell_grouplow")
summarize_simulation(sims_N, alpha = 0.05, original_estimates)
summarize_simulation(sims_N, alpha = 0.05, c(original_estimates,1))
